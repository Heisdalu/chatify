const DateGroup = ({ text }: { text: string }) => {
  return (
    <div className="font-[700] text-[0.8rem] rounded-[10px] mx-auto top-[100px] text-center p-[0.5rem] border-gray-100 border-[1px] bg-white">
      {text}
    </div>
  );
};
export default DateGroup;
